<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_facturx" model="ir.ui.view">
        <field name="name">account.move.form.inherit.facturx</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <!-- Only show on Vendor Bills (in_invoice) in Draft state -->
                <button name="action_import_facturx_pdf" 
                        string="⚡ Import Factur-X" 
                        type="object" 
                        class="oe_highlight" 
                        attrs="{'invisible': ['|', ('move_type', '!=', 'in_invoice'), ('state', '!=', 'draft')]}"/>
            </xpath>
        </field>
    </record>
</odoo>
