[pytest]
# Exclude the internal_tools folder because it contains scripts (test_api_simple.py) 
# that run immediate code and crash the collector if the server is offline.
addopts = --ignore=tests/internal_tools
